import { DisplayMoneyPipe } from './display-money.pipe';

describe('DisplayMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
